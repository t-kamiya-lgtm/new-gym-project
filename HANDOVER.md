# 引継ぎ書: ジムアフィリエイト管理システム 方向転換の検討

## 0. このドキュメントについて

本ドキュメントは、既存の「ジムアフィリエイト管理システム(gym-system)」について、以下2点の大きな方向転換を検討するために作成された引継ぎ資料である。

1. 受注データの取得元を、決済チャットボット(pm-chat-bot)から**スマレジECリピート**へ変更する
2. 実績の紐付け方法を、**クーポンコード運用**から**広告コードを使ったURL別実績集計**へ変更する

現行システムは稼働中であり、今回の検討はあくまで方向転換の**可能性・実現方式の検討**であって、現行システムをすぐに置き換えることが決まっているわけではない(その前提でよいかは、検討開始時にユーザーに確認すること)。

このリポジトリ(`new-gym-project`)は、上記の方向転換を検討・構築するための新しいプロジェクトの器として用意されたものである。現行の `gym-system` リポジトリとは別リポジトリであり、既存システムのコードはここには含まれない。

---

## 1. 現行システム(gym-system)の概要

新しいセッションが前提知識として使えるよう、現行システムのビジネスモデル・アーキテクチャ・受注〜報酬計算の流れを要約する。

### 1.1 ビジネスモデル

- 運営会社: 株式会社プライムダイレクト(商品: プロテインモンスター)
- 代理店(ジム)経由のアフィリエイトプログラム。全体の流れ:
  1. 代理店(ジム)へ営業
  2. 問い合わせがあったジムの実態・SNSを確認
  3. クーポンコードを発行し、ジムへ案内
  4. 10食セット・店舗POP(会員向けサイトQR・クーポンコード印刷入り)を発送
  5. ジムで会員への営業(販売)を開始
  6. 会員がQRから会員向けサイトへアクセスし、クーポンコードを入力して購入
  7. 月末締めで受注実績を集計し、支払い明細書を発行してジム側に確認・同意してもらう
  8. 翌月末に報酬をお支払い
- 報酬単価は、**各店舗の当月合計購入点数(pt)に応じて店舗ごとに**決まる(1〜9pt=300円、10〜99pt=450円、100pt以上=600円)。法人単位で表示する「平均単価」は、店舗ごとの単価をポイント数で加重平均した参考値。

### 1.2 現行のシステム構成

- Next.js(App Router)+ Supabase(Postgres/Auth)+ Vercelホスティング。リポジトリ: `t-kamiya-lgtm/gym-system`
- **決済チャットボット(pm-chat-bot)と同一のSupabaseプロジェクト(DB)を共有**している。pm-chat-bot側の会員購入導線・注文処理はそのまま活用し、gym-system側は`gym_`接頭辞のテーブル群で独自にアフィリエイト管理機能を追加する形。
- 店舗とクーポンコードは1対1で紐付け、注文のcoupon_idから「どの店舗経由の注文か」を判定している(`gym_store_coupons`テーブル)。
- 受注データの取り込みは、共有DB上の`orders`テーブルに対するPostgresトリガー+`pg_net`によるWebhook経由で、pm-chat-bot側のアプリコードを一切変更せずにgym-system側のVercel API(受注作成・出荷・キャンセルの3種類)を呼び出す方式。

### 1.3 現行の受注〜報酬計算の流れ(データモデルの要点)

- `gym_order_lines`(受注明細台帳): 受注が**発生した時点**(注文確定時)で記録され、その後の出荷・キャンセルに応じて`shipment_flag`(not_shipped/shipped/canceled/excluded)が更新される。手動追加行(`is_manual`)、月末確定後のキャンセルに対応する返品行(`is_reversal`、マイナス点数)にも対応。
- 月末確定(`closeMonthForCorporation`): 対象月の出荷済み受注明細を店舗ごとに集計し、店舗ごとの当月合計点数から店舗ごとの単価・報酬額を算出。`gym_monthly_statements`・`gym_monthly_statement_stores`にスナップショットとして保存し、以降は数値を固定する。
- パートナー法人側は、確定後の明細を確認して同意する(同意前は「未確定」または「確定済み・同意待ち」)。問い合わせがあれば運営側が調査し、フラグ解除して再確定できる。
- 支払い明細書はインボイス制度対応PDFで発行(発行元情報、宛名・登録番号、店舗別内訳、振込予定日の自動計算(土日祝考慮)、振込先銀行口座、振込手数料の負担についての注記など)。
- 会員データは、pm-chat-bot側の`customers`テーブル(氏名・性別・生年月日等を保持)をそのまま参照している(現行は会員個人情報を保有する前提の設計)。

### 1.4 主要ファイル(参考: 現行 `gym-system` リポジトリ内)

- `src/lib/statements.ts`: 月次締め・報酬計算のコアロジック
- `src/lib/order-lines.ts`: 受注明細台帳の読み書き
- `src/lib/points.ts`: ダッシュボード等で使うライブ集計(orders/customers/productsを直接参照)
- `src/lib/pdf/StatementDocument.tsx`: 支払い明細書PDF
- `public/manual.html` / `public/manual-admin.html`: パートナー向け/運営向けマニュアル
- DBスキーマ: `supabase/migrations/`配下(`gym_`接頭辞のテーブルはgym-system独自、`orders`/`customers`/`products`/`subscriptions`/`coupons`はpm-chat-bot側の共有テーブル)

---

## 2. 今回の方向転換の内容

### ユーザーからの指示(原文)

> こちら、方向性転換の相談です。
> チャットボットを使用せず、スマレジECリピートにて管理することになります。
> また、クーポンコード運用ではなく、広告コードを使ったURL別実績集計になる。
> その前提で、スマレジECの該当の受注をAPIで拾って、法人、店舗別案で集計を行うシステム構築は可能か。
> 会員情報は、どのジム経由で、どの会員が何を、いつ、何個が見られるようにして、個人情報は取り込まない。

### 要件の整理

1. **受注データソースの変更**: pm-chat-botではなく、スマレジECリピートで受注管理を行う。新システム側は、スマレジECの該当受注をAPI経由で取得する。
2. **実績の紐付け方法の変更**: クーポンコード(店舗1:1)ではなく、**広告コードを使ったURL別**の実績集計にする(店舗ごとに異なる広告コード付きURLを発行し、そのURL経由の注文を店舗に紐付ける想定と思われる。具体的な計測方法は要調査・要確認)。
3. **集計軸**: 法人・店舗別に受注実績を集計できること。
4. **可視化したい情報**: **どのジム経由で・どの会員が・何を・いつ・何個**購入したかが分かること。
5. **個人情報を取り込まない**: 会員の氏名・連絡先等の個人情報はシステム側に取り込まない。会員は何らかの非個人情報な識別子(会員ID等)で区別できればよいと解釈できる(取り込み範囲の具体的な線引きは要確認)。

---

## 3. 現行資産のうち、流用できる可能性がある部分

以下は受注データソースに依存しない、または依存を切り離せば流用できる可能性がある部分:

- 報酬単価ロジック(店舗単位の当月合計点数に応じたtier判定、法人単位の加重平均単価の考え方)
- 月次締め業務フロー全体の設計思想(未出荷相当のチェック → 月末確定 → パートナー確認・同意 → 問い合わせ対応 → 再確定)
- 支払い明細書PDF生成(インボイス対応、振込予定日の自動計算、店舗別内訳)
- 運営側・パートナー側の権限モデル、複数法人ログインの考え方
- パートナー向け/運営向けマニュアルの構成・運用方針

一方、以下は受注データソースの変更に伴い、**作り直しが必要**になる可能性が高い:

- 受注取り込み層(現行はDBトリガー+pg_net。スマレジEC側は別APIになるため、ポーリングまたはWebhook受信の新規実装が必要)
- 店舗と実績の紐付けキー(`gym_store_coupons` → 広告コード/URLベースの新しいマッピング)
- 会員データの扱い(現行は`customers`テーブルを直接参照。個人情報を持たない前提の新しいデータモデルが必要)
- 出荷・キャンセル状態の管理(スマレジEC側のステータス体系に合わせて再設計が必要)

---

## 4. 検討・調査が必要な論点

**特に重要なのは以下の点で、これらは本ドキュメント作成側では判断できず、別途調査・ヒアリングが必要である。**

### 4.1 スマレジECリピート API 【調査済み】

株式会社プライムダイレクトの受注APIドキュメント(カートAPI・受注API・カートコンバージョンAPI)を確認した。判明事項:

- 受注データは `POST/GET https://www.primedirect.jp/api/v2/orders/search` で取得可能。`order_date_from/to`・`update_date_from/to`・`order_statuses` 等で絞り込み、`limit`(既定500)・`offset`でページングするバッチ/ポーリング型API。**Webhookの提供は本資料には記載なし**(→未確定、要追加確認)。
- 受注オブジェクトに `order.advertising_group_code`(直接ADコード)・`order.first_advertising_group_code`(初回ADコード)・`order.referrer_url`(流入URL)・`order.referrer_url_param`(流入パラメーター)が標準項目として存在する。→ 4.2で確定。
- キャンセル関連は `order.cancel_date`、`order.order_status`/`order_status2`、明細単位の `order_detail` がある。ステータスの実際のマスタ値一覧は別途確認が必要(4.1の残課題)。
- `orders/search` の `search_fields`/`except_fields` で取得項目を選別できるため、氏名・電話・メール・住所等のPII項目をAPI呼び出し時点で除外して取得することが技術的に可能。
- 会員関連フィールド: `order.customer_id`(EC会員ID)、`order.smaregi_customer_id`/`smaregi_customer_code`(スマレジ会員ID/コード)など非PIIの識別子候補が複数存在する。氏名・メール・電話・住所・生年月日等のPIIフィールドも同一オブジェクトに含まれるため、**取得時に除外する運用(except_fields指定)が必須**。

**残っている調査事項**: Webhookの有無、APIレート制限値、認証(access_token)の発行方法・IP制限、テスト/サンドボックス環境の有無、`order_status`等マスタ値一覧。詳細は5.3のチェックリスト参照。

### 4.2 広告コードの設計 【方針確定】

ユーザーへのヒアリングにより以下が確定した:

- 広告コード(`advertising_group_code`)は**スマレジ管理画面で発番・設定**する(運営側で個別登録が可能。1店舗1コードの固定運用が可能)。
- アフィリエイト(代理店)経由の識別という用途への転用は**問題なし**(スマレジ側の想定利用に反しない)。
- 会員導線は「**注文フォーム一体型LP**」に変更される。店舗ごとに広告コードが紐づいた注文フォーム一体型LPを発行し、**会員がそのLPでフォーム入力した時点で会員登録と注文が同時に完了する**(現行の「QR→会員サイトログイン→クーポンコード入力→購入」という多段の会員導線がなくなり、LPアクセス自体が店舗識別を兼ねる)。
- 店舗紐付けは、**カートコンバージョンAPI(外部ASPタグ出力用)を経由せず**、`orders/search`で取得した `advertising_group_code` を新システム側の店舗マスタと突合するだけで完結する見込み(受注データに広告コードが直接入るため)。

→ この結果、現行の `gym_store_coupons`(クーポンコード1:1)に相当する仕組みは、「店舗マスタに `advertising_group_code` を1:1で登録するテーブル」にそのまま置き換えられる。クーポン運用からの設計変更としては軽微。

### 4.3 会員(個人情報)の扱い

- 「個人情報を取り込まない」の具体的な線引き(例: 氏名・住所・電話番号・メールアドレスは除外し、会員ID・性別・年代・購入履歴のみ保持する、等)を明確化する
- スマレジ側の会員IDやハッシュ化した識別子を、新システム側の「会員」の代替キーとして使えるか
- 個人情報保護方針・社内規定上、取得・保持してよい範囲の確認が必要か

### 4.4 既存データ・パートナーの移行方針

- 既存のpm-chat-bot経由の実績データ(過去分・過去の支払い明細)をどう扱うか(現行方式のまま保持し、以降の新規分のみスマレジ方式に一本化するのか、一定期間の並行運用があるのか)
- 対象パートナー法人・店舗が全社一斉切り替えか、順次移行か
- 移行期間中、クーポンコード方式と広告コード方式が混在する可能性があるか

### 4.5 システム構成

- 新規に別リポジトリ/別システムとして構築するか、gym-system内の受注取り込み層だけを差し替えるか(→ 本検討では新規リポジトリ `new-gym-project` として立ち上げ済み)
- 既存のSupabase(pm-chat-botと共有)を使い続けるか、新しいデータベースを用意するか(スマレジEC移行後もpm-chat-botとのDB共有関係を維持する理由がなくなる可能性がある点に留意)

---

## 5. 次のアクション案と現行リポジトリの参考情報

### 5.1 次のアクション(最初の一歩案)【実施済み・更新】

1. ~~スマレジECリピートのAPI仕様を確認し、受注取得・広告コード/流入元データの有無を調査する~~ → 完了(4.1参照)
2. ~~「広告コード」の具体的な運用イメージをユーザーにヒアリングする~~ → 完了(4.2参照)
3. 個人情報を取り込まない前提でのデータモデル(会員の代替キー、保持する属性の範囲)を設計する → `new-gym-project`リポジトリで着手(5.3参照)
4. 現行gym-systemの報酬計算・月次締め・明細発行ロジックの再利用可否を評価する → 流用方針で着手
5. 基本設計案(データモデル・API連携方式)をコードベースで具体化し、ユーザーに確認しながら実装を進める → 現在進行中

### 5.3 実装着手にあたり確認が必要な項目チェックリスト

**必須(データモデル・同期ロジックの実装に直接影響)**

- [x] スマレジEC APIの `access_token` 発行方法 → **判明・実装済み**。OAuth2認可コードフロー(基本設定 > 外部アプリ連携)。手順は5.3.1参照。**実際にトークンを取得する作業(管理画面でのアプリ登録+認可操作)はまだ未実施**
- [x] 広告コードの命名規則・初期の法人/店舗一覧 → **ダミー法人・店舗で仮登録済み**(`supabase/migrations/0002_seed_dummy_and_products.sql`)。実在の法人・店舗・広告コードが決まり次第、本データに差し替える
- [x] 対象商品と換算ルール → **確定**。プロテインモンスター(高タンパク麺)・プロテインモンスターソバ、それぞれ10食セットの単品/定期で計4品番。すべて「10食セット=1pt」で換算(現行の考え方を踏襲)。ただし**実際の`product_code`(スマレジ品番)は未確定のため、`products`テーブルには仮コードを登録済み**。実品番が判明次第差し替えが必要
- [x] 単価テーブル → **現行(1〜9pt=300円、10〜99pt=450円、100pt以上=600円)を踏襲することで確定**
- [x] 会員の非PII識別子 → **`order.customer_id`(primedirect.jp/EC側の会員ID)を採用することで確定**。`smaregi_customer_id`はスマレジPOS(実店舗レジ)側のIDで、オンライン注文のみの今回はほぼ値が入らないため不採用。保持する属性は「会員ID(customer_id)+購入商品+数量+日時+店舗」の範囲で運用
- [ ] `order_status`/`order_status2`/`payment_status` の実際のマスタ値一覧(出荷済/未出荷/キャンセル相当へのマッピング用)とキャンセル・返品が伝票単位か明細単位かの実運用

**後回し可(初期のスキーマ・ロジック実装には不要、後日確定でよい)**

- [ ] Webhook/プッシュ通知APIの有無(なければポーリング間隔は仮決めで進め、後日調整)
- [x] テスト/サンドボックス環境の有無 → **無し**(本番環境のみ)
- [ ] ホスティング・インフラ方針(本ドキュメントでは Vercel + 新規Supabaseプロジェクトを既定案として進める)
- [ ] 既存gym-systemからの移行方針(過去データの扱い、並行運用期間)

### 5.3.1 アクセストークン取得手順(実装済み・要実施)

スマレジEC・リピートの外部アプリ連携はOAuth2の認可コードフロー。`new-gym-project`側に実装済みのエンドポイントと合わせた手順は以下:

1. **プライムダイレクト運営側の管理者**が、スマレジEC管理画面「基本設定 > 外部アプリ連携」でアプリケーションを新規登録する。
   - アプリケーション名: 任意(例:「ジムアフィリエイト集計システム」)
   - リダイレクトURL: `https://<デプロイ先ドメイン>/api/smaregi/oauth/callback`(本リポジトリに実装済み)
   - IP制限: 未設定を推奨(Vercelのサーバーレス関数は固定IPを持たないため)
   - 登録すると `client_id` / `client_secret` が発行される
2. 発行された値を新システムの環境変数に設定する: `SMAREGI_CLIENT_ID`、`SMAREGI_CLIENT_SECRET`、`SMAREGI_REDIRECT_URI`(上記と同じURL)、`SMAREGI_OAUTH_BASE_URL`(`https://www.primedirect.jp`を仮設定)
3. 管理者が(ログイン済みブラウザで)`https://<デプロイ先ドメイン>/api/smaregi/oauth/start` にアクセスする → スマレジ側の認可ページにリダイレクトされる → 許可する
4. 認可後、自動的に `/api/smaregi/oauth/callback` に戻り、新システムがアクセストークンを取得して `smaregi_oauth_tokens` テーブルに保存する(**認可コードは一度しか使えないため、取得後は自動保存する実装にしてある**)
5. 以降の受注同期(`syncOrders()`)は、保存されたアクセストークンを自動的に使用する

scopeは受注参照のみに必要な `read_sales` を指定する実装にしてある(在庫更新等の書き込み権限は要求しない)。

**この手順の実施には、プライムダイレクト側のスマレジEC管理画面の管理者権限が必要**です。この作業は運営側で行っていただく必要があります。

### 5.3.2 補足: システム構成についての重要な追加情報

ユーザーからの補足により、以下が判明した:

- 「スマレジEC・リピート/一元管理」はECカートシステムであり、`primedirect.jp`はそのスマレジ上に構築されたサイトである。
- **スマレジの受注データはAPIで「通販ゲート」的な中間システムに連携されており、これまで示されたカートAPI/受注API/カートコンバージョンAPIは、スマレジ本体ではなくその連携先(スマレジとは別サービス)が提供するものである。**
- これにより、`order.smaregi_customer_id`のような「スマレジ側ID」を持つ項目は、あくまで連携元がスマレジPOSであることを示す参照情報であり、EC単独注文では基本的に空になる、という上記の会員キー判断の裏付けが取れた。
- ログインAPI(`POST /api/v2/cart/login`)の存在から、`order.customer_id`はEC会員登録(今回は「フォーム入力=会員登録」のLP経由)によって必ず払い出されるIDであることが確認できた。

### 5.4 参考: 現行リポジトリ情報

- リポジトリ: `t-kamiya-lgtm/gym-system`(Next.js App Router / Supabase / Vercelホスティング)
- 開発ブランチ運用: `claude/gym-affiliate-management-g9hchw` で開発 → PR作成 → マージ → ブランチ同期、というフローで進めてきた
- DBマイグレーション: `supabase/migrations/`配下に連番SQLファイルとして管理。CIでの自動適用はなく、都度Supabase側で手動実行してもらっている
- 主要ファイル: `src/lib/statements.ts`、`src/lib/order-lines.ts`、`src/lib/points.ts`、`src/lib/pdf/StatementDocument.tsx`、`public/manual.html`、`public/manual-admin.html`
